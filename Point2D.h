#pragma once

struct Point2D {
    int x; // X-coordinate
    int y; // Y-coordinate

    // Constructor Initializer
    Point2D(int initialX, int initialY) : x(initialX), y(initialY) {}
};

